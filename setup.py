from setuptools import setup

setup(
    name="partition",
    version="0.0.1a",
    description="MBR/GPT parsing tool.",
    author="KIodine",
    packages=["partition",],
    python_requires=">=3.7",
)